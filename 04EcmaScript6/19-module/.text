나는 하나만 빼서 쓰고싶은데 한 파일로 다 연결하면 불필요한 리소스가 낭비된다
그래서 모듈을 쓰자.
호출을 할때 모듈방식을 사용하고싶다면, 
1. http환경에서만 돌아감. 서버환경에서만 동작한다.
2. type="module"사용
3. defer랑은 같이 사용하지 못함
4. inport , export라고 생각하면 됨.
5. 사용하고자 하는 변수 앞에 



6. 스크립트는 순서를 맞춰야하는데 이건 그럴필요가 없다. 스크립트에서 필요한것만 호출하니까 리소스가 절감이 된다. 번들러.요즘트렌드 스크립트파일을 여러개 쓰면 이걸 하나로 합치질 못함. 합치면 속도가 빨라짐. 합칠려면 번들러를 사용하는데, 모듈방식일때 파일을 합치는게 용이함 그래서 디플로이 하기위해서.....

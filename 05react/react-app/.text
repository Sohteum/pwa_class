라우터

페이지를 전환. 주소를 이용해서 특정 컴포넌트를 보여주는걸 라우터라고 함
주로 큰사이즈를 할때 라우터를 이용함

작은건 삼항식 조건부출력



npm i react-router-dom  
라우터는 설치를 해야 쓸수잇음


앱에서 해봤자 컨텍스트를 못만듦 익스폴트를 못하니까.
그럴려면 밖에서 만들어야하는데 그래서 여기서 싸는게 아니고(컨텍스트 사용안하면 앱에서도 사용가능)

인덱스가 더 큰 범위기때문에 인덱스에서 사용하면 된다.
  <BrowserRouter>이거


  주소가 바뀐때 컴포넌트가 변하는 부분을 라우터 주소를 바꿔가지고 사용하는거.
        <Routes>
      <CompHome />
      <CompDetail />
      <CompAddList />
      </Routes>

      이렇게 못쓰기때문에 안에있는 컴포넌트를 모두 지워준다.
            <Routes>
        <Route />
        <Route />
        <Route />
      </Routes>

      대신에 컴포넌트가 라우트가 됨.
      임폴트해야함 엔터치라는 소리져

      패스하고 엘리먼트속성 무조건 두가지를 해줘야함


path 시작경로일때. 보여주고싶은걸 엘리먼트라고 함



        <Route path="/" element={<CompHome/>}/>

      이게 첫페이지를 의미함 = http://localhost:3000


배열이 있어야 목록을 출력하니까 앱에서 통으로 관리를 해줘야함.
앱을 하나 만들거야
us

맵은 동일한 길이인데 조건에 맞는 전체 길이를 가져오는거고
만약에 이름, 점수면 그걸 다 가져올수 있ㄴ느데, 맵.이름, 맵.점수 이렇게 원하는것만 가져올수있음
필터는 내가 요구한 조건만 가져옴  li  전체를 가져옴. 다섯개가있는데 그중에 세개만 조건에 맞는 세개만 가져옴
점수가 60점이상인애
이런식으로 조건을 걸어서 사용할때 주로 쓸수있다.

이제 목록추가할때마다 배열을 하났기 추가하는건데 거기가 투두어래이인거지 그러면 이앤더링이 되고, 홈이 바뀌는거지
목록이 추가되면 부모>자식 변함


     <li>
      <p><Link to={'/detail'}>{title}</Link></p> 
    </li>
      라우터를 설치해야 할수잇음-> 그리고 타이틀을 받아오기!!!!


키는 프롭으로 받을 수 있는게 아님

해당 v번째 아이디를 프롭으로 내려주자

find는 값을 찾는다고요???
배열의 객체중 ㅍ번째에 id가 parameter로 받은 id와 동일한 객체를 찾음
파라미터랑 브이.아이디랑 같은애를 찾는다. 그럼 해당 객체가 나온다. 그걸 찾는게 파인드